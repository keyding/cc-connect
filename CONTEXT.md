# CC-Connect 协作会话领域语言

用于 Telegram 项目协作的已确认术语。这里描述目标领域含义，不表示相关功能已经实现。

## Language

**共享会话**：同一 Telegram 群、同一项目内，授权用户可共同选择和使用的对话及任务上下文；每个会话固定属于一种 Agent。会话身份独立于名称，从待开始到已启动、改名及重启恢复后保持不变。
_Avoid_: 用群、Topic 或某位用户的当前选择指代会话本身

**会话名称**：共享会话供成员辨认的可修改名称，在同一群、同一项目内跨 Topic 唯一；名称首尾空白及英文字母大小写不构成区别。
_Avoid_: 将可修改的名称作为会话身份

**待开始会话**：已命名并可供成员选择，但尚未执行第一项任务的共享会话。
_Avoid_: 不存在的会话

**当前默认会话**：某个项目及群聊入口中，未指定其他目标的普通消息所使用的会话，可以处于未选择状态。个人选择模式下由每位用户在每个 Topic 独立选择；共同选择模式下由同 Topic 的授权用户共用选择。回复旧 Bot 消息所指定的目标不会改变默认选择。
_Avoid_: 将跨 Topic 的共享会话目录等同于全群唯一默认选择

**会话请求**：一位用户提交给某个会话的一项任务，具有明确的发起人和回复位置。
_Avoid_: 将请求与会话混用

**会话工作区**：会话操作代码的独立工作环境。同一共享会话的成员使用同一个工作区。
_Avoid_: 将项目目录与会话工作区混用

**恢复点**：用于恢复会话及其工作成果的一组相互对应的备份数据。
_Avoid_: 将聊天历史副本直接称为完整恢复点
